# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET    /nextWordPrediction    controllers.NextWordPrediction.nextWordPrediction(sentence: String, callback: String)
